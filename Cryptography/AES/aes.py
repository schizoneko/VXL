import os
import struct

# AES S-box
SBOX = [
    
]

# AES inverse S-box
INV_SBOX = [
    
]

# Round constants
RCON = [0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36]

def sub_bytes(state):
    return [SBOX[b] for b in state]

def inv_sub_bytes(state):
    return [INV_SBOX[b] for b in state]

def shift_rows(state):
    s = [state[i*4:(i+1)*4] for i in range(4)]
    return [
        s[0][0], s[0][1], s[0][2], s[0][3],
        s[1][1], s[1][2], s[1][3], s[1][0],
        s[2][2], s[2][3], s[2][0], s[2][1],
        s[3][3], s[3][0], s[3][1], s[3][2]
    ]

def inv_shift_rows(state):
    s = [state[i*4:(i+1)*4] for i in range(4)]
    return [
        s[0][0], s[0][1], s[0][2], s[0][3],
        s[1][3], s[1][0], s[1][1], s[1][2],
        s[2][2], s[2][3], s[2][0], s[2][1],
        s[3][1], s[3][2], s[3][3], s[3][0]
    ]

def mix_columns(state):
    def mul(a, b):
        p = 0
        for _ in range(8):
            if b & 1:
                p ^= a
            hi_bit = a & 0x80
            a <<= 1
            if hi_bit:
                a ^= 0x1b
            a &= 0xff
            b >>= 1
        return p & 0xff

    s = []
    for i in range(4):
        c = state[i*4:(i+1)*4]
        s.extend([
            mul(c[0], 2) ^ mul(c[1], 3) ^ c[2] ^ c[3],
            c[0] ^ mul(c[1], 2) ^ mul(c[2], 3) ^ c[3],
            c[0] ^ c[1] ^ mul(c[2], 2) ^ mul(c[3], 3),
            mul(c[0], 3) ^ c[1] ^ c[2] ^ mul(c[3], 2)
        ])
    return s

def inv_mix_columns(state):
    def mul(a, b):
        p = 0
        for _ in range(8):
            if b & 1:
                p ^= a
            hi_bit = a & 0x80
            a <<= 1
            if hi_bit:
                a ^= 0x1b
            a &= 0xff
            b >>= 1
        return p & 0xff

    s = []
    for i in range(4):
        c = state[i*4:(i+1)*4]
        s.extend([
            mul(c[0], 14) ^ mul(c[1], 11) ^ mul(c[2], 13) ^ mul(c[3], 9),
            mul(c[0], 9) ^ mul(c[1], 14) ^ mul(c[2], 11) ^ mul(c[3], 13),
            mul(c[0], 13) ^ mul(c[1], 9) ^ mul(c[2], 14) ^ mul(c[3], 11),
            mul(c[0], 11) ^ mul(c[1], 13) ^ mul(c[2], 9) ^ mul(c[3], 14)
        ])
    return s

def add_round_key(state, round_key):
    return [s ^ k for s, k in zip(state, round_key)]

def key_expansion(key):
    def rot_word(w):
        return w[1:] + w[:1]

    def sub_word(w):
        return [SBOX[b] for b in w]

    keys = []
    w = [key[i:i+4] for i in range(0, 16, 4)]
    keys.extend([b for word in w for b in word])

    for i in range(4, 44):
        temp = w[-1]
        if i % 4 == 0:
            temp = sub_word(rot_word(temp))
            temp[0] ^= RCON[i//4 - 1]
        w.append([a ^ b for a, b in zip(w[-4], temp)])
        keys.extend([b for b in w[-1]])

    return [keys[i:i+16] for i in range(0, len(keys), 16)]

def encrypt_block(block, round_keys):
    state = list(block)
    state = add_round_key(state, round_keys[0])

    for i in range(1, 10):
        state = sub_bytes(state)
        state = shift_rows(state)
        state = mix_columns(state)
        state = add_round_key(state, round_keys[i])

    state = sub_bytes(state)
    state = shift_rows(state)
    state = add_round_key(state, round_keys[10])

    return bytes(state)

def decrypt_block(block, round_keys):
    state = list(block)
    state = add_round_key(state, round_keys[10])

    for i in range(9, 0, -1):
        state = inv_shift_rows(state)
        state = inv_sub_bytes(state)
        state = add_round_key(state, round_keys[i])
        state = inv_mix_columns(state)

    state = inv_shift_rows(state)
    state = inv_sub_bytes(state)
    state = add_round_key(state, round_keys[0])

    return bytes(state)

def pad_data(data):
    padding_len = 16 - (len(data) % 16)
    return data + bytes([padding_len] * padding_len)

def unpad_data(data):
    padding_len = data[-1]
    return data[:-padding_len]

def encrypt(data, key):
    round_keys = key_expansion(key)
    padded_data = pad_data(data)
    encrypted = b""
    for i in range(0, len(padded_data), 16):
        block = padded_data[i:i+16]
        encrypted += encrypt_block(block, round_keys)
    return encrypted

def decrypt(data, key):
    round_keys = key_expansion(key)
    decrypted = b""
    for i in range(0, len(data), 16):
        block = data[i:i+16]
        decrypted += decrypt_block(block, round_keys)
    return unpad_data(decrypted)

def main():
    # Fixed 128-bit key (16 bytes)
    key = b"16bytekey1234567"
    
    # Ensure input.txt exists in the same directory as the script
    script_dir = os.path.dirname(os.path.abspath(__file__))
    input_path = os.path.join(script_dir, "input.txt")
    
    # Read input
    try:
        with open(input_path, "rb") as f:
            data = f.read()
    except FileNotFoundError:
        print(f"Error: 'input.txt' not found in {script_dir}")
        return
    
    # Encrypt
    encrypted = encrypt(data, key)
    with open(os.path.join(script_dir, "encrypted.txt"), "wb") as f:
        f.write(encrypted)
    
    # Decrypt
    decrypted = decrypt(encrypted, key)
    with open(os.path.join(script_dir, "decrypted.txt"), "wb") as f:
        f.write(decrypted)

if __name__ == "__main__":
    main()